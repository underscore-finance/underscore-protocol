# Wallet v3 Tests

This directory is reserved for tests of the new Wallet v3 implementation.

The paused proof-of-concept suite is archived under
[`tests/poc/userWallet/`](../poc/userWallet/). Its passing tests demonstrate the
old experiment only; they are not acceptance evidence for the new
architecture.
