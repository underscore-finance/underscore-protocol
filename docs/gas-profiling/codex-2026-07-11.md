# User Wallet Transfer Gas Profiling and Optimization Report

**Date:** 2026-07-11  
**Repository:** `underscore-v2`  
**Branch at document creation:** `wallets-v3` (the commit hash below is the authoritative code reference)  
**Commit analyzed:** `cce97d56b62f58c0c3eeddb8fc551d1723bf0fc3`  
**Primary scope:** User-wallet payments through `UserWallet.transferFunds`, including whitelist transfers, registered payees, cheques, Billing pulls, and agent-mediated payments  
**Revision:** 2 — independently reproduced and amended after review on 2026-07-11\
**Status:** Analysis and recommendations only; no protocol contracts were modified

---

## 1. Executive Summary

The actual ETH or ERC20 transfer is not the primary gas cost in the user-wallet payment flow. Most gas is consumed by the authorization, pricing, configuration, recipient-limit, wallet-accounting, and deposit-points machinery that surrounds the transfer.

The measured **same-block repeat** owner-to-whitelisted-recipient ERC20 payment costs:

- `351,839` gas of contract execution.
- `373,839` gas as a direct-transaction-equivalent result after adding intrinsic transaction gas.
- Only approximately `10,194` gas for the repeat ERC20 `transfer` itself.

The strongest single finding on the measured baseline—an owner that is not also indexed as a manager, with `canOwnerManage` enabled—is that the permission check loads three large manager data structures before Sentinel takes the owner shortcut:

| Owner-path manager retrieval whose full structure is unnecessary | Measured gas |
|---|---:|
| Global manager settings getter; only `canOwnerManage` is needed | 58,201 |
| Signer-specific manager settings getter | 56,625 |
| Manager period data getter | 15,488 |
| **Total** | **130,314** |

This is approximately:

- `37.0%` of the same-block repeat owner transfer's contract execution.
- `34.9%` of its direct-transaction-equivalent gas.

The highest-priority optimization is therefore an owner-specific authorization fast path that runs before manager structs are retrieved. The current three-getter burden is directly measured at `130,314` gas. For the common owner-not-manager path, the eventual net saving is modeled at roughly `120,000–150,000` gas because a replacement must still read `canOwnerManage` and will change surrounding ABI/call costs.

Owner and manager classification are independent. Ownership can be transferred to an address that is already a registered manager. If such an owner-manager has `canOwnerManage = true`, pre-authorization takes the owner shortcut, but `ad.isManager` remains true and manager post-accounting still runs. If `canOwnerManage = false`, the owner-manager falls through to full manager authorization. The `120,000–150,000` estimate does not apply unchanged to either owner-manager branch and both require separate measurement.

Other major conclusions are:

1. Registered-payee accounting is expensive because it loads and rewrites an eight-field `PayeeData` structure. For the measured matching-primary-asset payee, it costs approximately `235,890` gas on first use and `93,490` gas on same-block repeat. A no-primary-asset owner payment was `40,114` gas cheaper on first use and `5,914` cheaper on same-block repeat.
2. Manager post-transfer accounting costs approximately `132,253` gas on first use and `24,053` gas on same-block repeat.
3. An owner who is also a registered manager bypasses manager pre-rules when the owner shortcut applies, but still faces specific/global manager USD caps after the transfer and still accrues manager counters. A failed post-check atomically reverts the complete transaction.
4. A cheque is expensive twice: creation costs `714,824` direct-transaction-equivalent gas, and later payment costs another `485,198–743,389` depending on the caller path.
5. In the historical first-use/no-block-travel comparison, the existing atomic agent `createAndPayCheque` route is `18.7–33.1%` cheaper than the measured two-transaction cheque product routes; a sequential mature-accounting follow-up measured `35.7%`. Both comparisons mix owner, Billing, and agent entry paths and neither isolates the saving caused solely by atomic execution.
6. The historical first-manager-use agent batch saved `284,269` gas (`29.7%`); a sequential mature-manager-state follow-up saved `284,293` gas (`33.4%`).
7. A normal partial transfer prices the outgoing amount and then prices the remaining wallet balance again. The two `isBasicEarnVault` checks alone consumed `26,886` gas.
8. Deposit-points accounting is performed on every non-eject transfer and can become materially more expensive when blocks advance and points actually accrue.
9. Billing pulls are approximately `116,000` gas cheaper than comparable owner pushes because Billing bypasses the large manager authorization structures earlier. The historical matching-primary result is `116,198`; a follow-up evolved-state no-primary cross-scenario subtraction is `116,226`, only `28` gas different, but is corroboration rather than a controlled paired comparison.
10. An exact-full native-token transfer currently reverts during asset deregistration. This is a correctness issue discovered during profiling and should be fixed independently of the optimization work.

The practical optimization order should be:

1. Fix exact-full native transfers and add regression coverage.
2. Add the owner authorization fast path.
3. Make basic-vault classification an O(1) lookup.
4. Use the existing batch and atomic-cheque routes wherever their semantics match the product action.
5. Introduce a compact transfer-specific authorization bundle.
6. Combine transfer and remaining-balance pricing for explicitly supported standard assets.
7. Pack high-frequency manager, payee, cheque, and points data in a new wallet/config version.
8. Build a true wallet-native batch and a non-persistent instant-cheque path.

Savings estimates in this report overlap. They must not be summed without remeasuring the combined implementation.

---

## 2. Scope and Questions Answered

This analysis answers the following questions:

- What is the complete on-chain path for a normal user-wallet transfer?
- Which subcalls and state transitions consume the most gas?
- How do whitelist, payee, cheque, manager, Billing, and agent paths compare?
- What is the difference between first-use, same-block repeat, and cross-block repeat gas?
- How much does batching save today?
- How much does the atomic cheque path save today?
- Which optimizations are low-risk, and which require a new security model or storage layout?
- Which improvements can benefit existing wallets, and which require new wallet/config templates?
- Which costs are wallet execution costs versus Base L1-data or account-abstraction sponsorship costs?

The analysis focuses on successful payment execution. It is not a complete protocol security audit, token-compatibility audit, or production sponsorship-provider audit.

The baseline uses a conventional non-yield mock ERC20 and local price infrastructure. Yield tokens, rebasing tokens, fee-on-transfer tokens, proxy-token implementations such as production USDC, and production oracle paths may produce different results and should be benchmarked separately.

---

## 3. Measurement Methodology

### 3.1 Environment

Measurements were produced with the repository's Titanoboa/Py-EVM environment at commit `cce97d56b62f58c0c3eeddb8fc551d1723bf0fc3`.

The benchmark harness used:

- Fresh wallets for independent scenarios.
- Mock ERC20 and MockRipe pricing infrastructure.
- Registered asset and initialized Ledger points during setup unless the scenario explicitly tested an untracked asset.
- Py-EVM `PragueState`, using Prague/post-Cancun gas accounting.

No payment benchmark was run against a Base mainnet fork. The results should therefore be treated as controlled protocol-execution measurements, not production cost receipts.

### 3.2 Transaction-boundary normalization

Gas tools can substantially understate or distort costs when multiple test actions share warm accounts, warm storage, or dirty storage within one simulated transaction context. The final profiling harness corrected for this by:

1. Completing wallet and scenario setup before measurement.
2. Locking setup storage as prior-transaction state so EIP-2200 `SSTORE` pricing and refunds used the correct original values.
3. Clearing EIP-2929 account and storage access state before each independent measured transaction.
4. Clearing transient storage before each independent measured transaction.
5. Explicitly prewarming the top-level transaction target and addresses `0x01–0x0a`, matching the historical harness's transaction/precompile assumptions.
6. Measuring the top-level mutating call and all child calls.
7. Retaining transaction-wide warmth only for the intentionally batched scenario.

This matters because a simple cumulative `boa.env.get_gas_used()` measurement after fixtures can make repeat calls appear cheaper for the wrong reasons. The current [`scripts/utils/capture_gas.py`](../../scripts/utils/capture_gas.py) is useful as a collection wrapper, but a permanent CI benchmark should explicitly enforce transaction boundaries and prior-state locking.

A real transaction also warms its sender/`tx.origin`, and Prague defines the precompile range through `0x11`. The historical harness did not explicitly model those details. None of the measured paths accesses the omitted sender account or BLS precompiles in a way that changes these results. The maintained follow-up benchmark warms the sender, target, and full `0x01–0x11` range.

### 3.3 Reported gas definitions

The report uses four quantities:

- **Gross execution:** EVM contract execution before applying gas refunds and excluding transaction intrinsic gas.
- **Refund:** The raw EVM refund counter reported by the measured computation.
- **Intrinsic:** `21,000 + 4 × zero calldata bytes + 16 × nonzero calldata bytes` for the modeled direct call.
- **Receipt-like:** `gross execution + intrinsic - applicable refund`.

The receipt-like calculation was:

```text
pre_refund_gas = gross_execution + intrinsic
applicable_refund = min(refund_counter, pre_refund_gas / 5)
receipt_like_gas = pre_refund_gas - applicable_refund
```

The `/ 5` cap follows the post-London 20% maximum refund rule in [EIP-3529](https://eips.ethereum.org/EIPS/eip-3529).

This formula does not explicitly apply the [EIP-7623](https://eips.ethereum.org/EIPS/eip-7623) calldata-cost floor. That floor is non-binding for every scenario in this report because contract execution is hundreds of thousands of gas above the calldata floor.

This receipt-like number models a direct transaction to the measured entry point. It does **not** model the complete sponsored UserOperation envelope.

### 3.4 First versus repeat

In this report:

- **First** means the recipient token balance is zero and the relevant recipient/payee/manager-side period data is uninitialized. Ordinary ERC20 first-use rows prime wallet-level asset and points state during setup; the explicitly labeled fully fresh untracked-native row is an exception.
- **Same-block repeat** means the relevant state is already nonzero and account/storage access is cold because it is a new transaction, but block-dependent fields such as `lastTxBlock` and `lastUpdate` may be rewritten to their existing values.
- **Cross-block repeat** means initialized state is exercised after advancing the block so block-dependent values and deposit-points state can genuinely change.
- **Full balance** means the transfer leaves the wallet with zero of that asset and triggers the deregistration path.
- **Untracked native** means the wallet has native value but does not yet have the asset registered in wallet accounting.

### 3.5 Deposit-points caveat

No block travel occurred between setup and the main first/same-block-repeat measurements. Consequently, `_getLatestDepositPoints` returned zero new accrued points, and block-dependent fields such as `lastTxBlock`, `lastUpdate`, and period timestamps could receive cheaper same-value writes, although the point structures and USD values were still read and written.

A normalized follow-up benchmark measured these sequential evolved-wallet receipt-like gas observations:

| Flow | Same-block repeat | Cross-block repeat | Observed delta |
|---|---:|---:|---:|
| Owner → whitelist | 373,839 | 385,307 | **+11,468** |
| Owner → matching-primary-asset payee | 467,601 | 481,869 | **+14,268** |
| Generic agent → whitelist | 416,554 | 430,822 | **+14,268** |

These are useful cross-block budgeting observations, but they are not cloned-state A/B measurements: the cross-block calls occur later on an evolved wallet. They therefore should not silently replace the historical same-block rows or be treated as controlled production baselines. A cloned-state benchmark and production Base telemetry remain necessary to isolate the exact block/points premium.

---

## 4. Core Transfer Flow

The primary entry point is [`UserWallet.transferFunds`](../../contracts/core/userWallet/UserWallet.vy#L144).

A successful normal payment follows this sequence:

1. `_validateCanTransfer` selects the resolved asset and the `TRANSFER` or `PAY_CHEQUE` action.
2. `_performPreActionTasks` asks `UserWalletConfig` to validate signer permissions and return the action bundle.
3. The wallet clamps the requested amount against native or ERC20 balance.
4. `Appraiser.updatePriceAndGetUsdValue` prices the outgoing amount.
5. The wallet validates the recipient as a whitelist recipient, registered payee, or cheque recipient.
6. The wallet executes native `send` or ERC20 `transfer`.
7. Manager limits are updated when the action bundle marks the signer as a manager.
8. The wallet reads the remaining asset balance.
9. The remaining balance is priced and `WalletAssetData` is updated, or the asset is deregistered when its balance reaches zero.
10. LootDistributor and Ledger update user and global deposit points.
11. The wallet emits `WalletAction`.

The most important observation is that step 6 is small relative to steps 2, 4, 5, and 7–10.

### 4.1 Signer branches

#### Owner

The owner does not bypass recipient rules. An owner payment must still target a whitelisted recipient, registered payee, or valid active cheque.

[`ActionDataProvider`](../../contracts/core/userWallet/ActionDataProvider.vy#L203) computes `walletOwner` and `isManager` independently. [`HighCommand.addManager`](../../contracts/core/walletBackpack/HighCommand.vy#L197) blocks registering the current owner as a manager, but [`Ownership.changeOwnership`](../../contracts/modules/Ownership.vy#L104) can later transfer ownership to an existing manager without clearing that manager record. The signer can therefore simultaneously be owner and manager.

The owner authorization shortcut occurs inside [`Sentinel`](../../contracts/core/walletBackpack/Sentinel.vy#L102), but [`ActionDataProvider`](../../contracts/core/userWallet/ActionDataProvider.vy#L155) evaluates and retrieves the manager arguments before Sentinel is called. For an owner with `canOwnerManage = true`, this eager evaluation creates the `130,314`-gas current three-getter burden described later.

Sentinel's pre-action behavior is asymmetric:

- Owner + `canOwnerManage = true`: take the owner shortcut and bypass manager activation, action permission, manager asset/payee/Lego allowlists, cooldown, and transaction-count limits.
- Owner-manager + `canOwnerManage = false`: fall through to the complete manager authorization path.
- Owner-not-manager + `canOwnerManage = false`: reject.

Post-action behavior uses `ad.isManager`, not owner status. An owner-manager therefore runs [`checkManagerLimitsPostTx`](../../contracts/core/userWallet/UserWalletConfig.vy#L426) even after taking the owner shortcut. For a normal transfer, the post-check applies both manager-specific and global `failOnZeroPrice`, per-transaction USD, per-period USD, and lifetime USD limits. Exceeding one occurs after the token/native send in execution order but atomically reverts the entire transaction, including the transfer and earlier recipient writes. On success, the manager transaction, period, lifetime, and `lastTxBlock` fields are updated. Count caps and cooldowns are pre-only, so the owner shortcut bypasses those checks even though the counters still advance.

This is current protocol behavior. Making owner status suppress manager post-accounting would be a separate authorization/accounting policy change, not a gas-only optimization.

#### Manager

Managers use the same general action bundle but must also satisfy:

- Manager activation and period timing.
- Transfer permission.
- Asset allowlists.
- Payee allowlists where applicable.
- Per-manager and global transfer limits.
- Post-transfer manager counters and totals.

Manager asset and payee checks can scan bounded arrays of up to 40 entries. Whitelisted recipients avoid the manager allowed-payee scan because ActionDataProvider rewrites the recipient for authorization purposes.

#### Billing

Billing receives a special early return in [`ActionDataProvider`](../../contracts/core/userWallet/ActionDataProvider.vy#L155). It therefore avoids the large manager settings and period-data getters. Recipient/payee/cheque validation still occurs inside the wallet/config path.

#### Agent

Agent calls add one or more layers before the wallet:

- `AgentSenderGeneric` or a signed sender endpoint.
- `AgentWrapper`.
- `UserWallet.transferFunds`.

Signed paths additionally perform message hashing, `ecrecover`, nonce validation/update, and signature-related calldata processing.

### 4.2 Recipient branches

#### Whitelisted recipient

This is the cheapest recipient class. Config checks the whitelist mapping, and Sentinel returns without persistent recipient-period writes. There is some duplicated whitelist work between signer authorization and recipient validation, but the second storage access is warm inside the transaction.

#### Registered payee

Config loads:

- Payee membership/index.
- Full `PayeeSettings`.
- Global payee settings.
- Eight-field `PayeeData`.

Sentinel validates activation, asset restrictions, cooldown/count limits, USD limits, and optional unit limits. A successful payment rewrites the full `PayeeData` structure at [`UserWalletConfig.vy`](../../contracts/core/userWallet/UserWalletConfig.vy#L516). When the transferred asset matches the payee's configured primary asset, all eight accounting fields can become nonzero; without a primary-asset match, the two unit-tracking fields remain zero. The historical payee measurements in Section 5 use the matching-primary-asset variant unless explicitly labeled otherwise.

#### Cheque

Config loads:

- The recipient's full cheque.
- Global cheque settings, including allowed assets.
- Global cheque period data.
- Owner/signer manager membership.

Sentinel validates active status, unlock and expiry, exact asset and amount, asset allowlists, price, cheque value, manager permissions, cooldown, transaction counts, and USD limits.

Successful payment updates cheque period data, decrements active cheque count, and clears the stored cheque.

---

## 5. Complete Benchmark Results

Rows not marked “follow-up” are retained from the original commit-pinned measurement ledger. Main first/repeat rows were measured without block travel; repeated rows are therefore labeled same-block repeat. Follow-up rows add independently reproduced state variants and do not overwrite the historical scenarios.

### 5.1 Direct wallet and native transfers

| Flow | Gross execution | Refund | Intrinsic | Receipt-like gas |
|---|---:|---:|---:|---:|
| Owner → whitelist ERC20, first | 368,939 | 0 | 22,000 | **390,939** |
| Owner → whitelist ERC20, same-block repeat | 351,839 | 0 | 22,000 | **373,839** |
| Owner → whitelist ERC20, full balance | 367,407 | 24,000 | 22,000 | **365,407** |
| Owner → tracked native, existing recipient | 341,286 | 0 | 22,000 | **363,286** |
| Owner → tracked native, empty recipient | 366,286 | 0 | 22,000 | **388,286** |
| Owner → untracked native, fully fresh registration state | 484,800 | 0 | 22,000 | **506,800** |
| Owner → matching-primary-asset payee, first | 605,048 | 0 | 22,000 | **627,048** |
| Owner → matching-primary-asset payee, same-block repeat | 445,601 | 0 | 22,000 | **467,601** |
| Owner → no-primary-asset payee, first (follow-up) | 564,934 | 0 | 22,000 | **586,934** |
| Owner → no-primary-asset payee, same-block repeat (follow-up) | 439,687 | 0 | 22,000 | **461,687** |
| Manager → whitelist, first | 505,428 | 0 | 22,000 | **527,428** |
| Manager → whitelist, same-block repeat | 380,234 | 0 | 22,000 | **402,234** |

The no-primary rows come from the maintained follow-up benchmark. Primary-asset unit tracking adds `40,114` gas to the first owner payment and `5,914` gas to the same-block repeat end-to-end flow. No isolated no-primary `PayeeData` component trace was captured, so those deltas should not be substituted directly for the component costs in Section 7.3.

### 5.2 Agent paths

| Flow | Gross execution | Refund | Intrinsic | Receipt-like gas |
|---|---:|---:|---:|---:|
| Generic agent → whitelist, first | 518,348 | 0 | 23,400 | **541,748** |
| Generic agent → whitelist, same-block repeat | 393,154 | 0 | 23,400 | **416,554** |
| Signed agent → whitelist, first | 546,656 | 0 | 24,624 | **571,280** |
| Generic agent → matching-primary-asset payee, first | 754,475 | 0 | 23,400 | **777,875** |
| Generic agent → matching-primary-asset payee, same-block repeat | 486,934 | 0 | 23,400 | **510,334** |

The signed-agent first transfer costs `29,532` gas more than the generic-agent first transfer. Part of the difference is execution for signature verification and nonce state, and part is larger calldata/intrinsic gas.

### 5.3 Billing pulls

| Flow | Gross execution | Refund | Intrinsic | Receipt-like gas |
|---|---:|---:|---:|---:|
| Billing matching-primary-asset payee pull, first | 488,850 | 0 | 22,000 | **510,850** |
| Billing matching-primary-asset payee pull, same-block repeat | 329,403 | 0 | 22,000 | **351,403** |
| Billing no-primary-asset payee pull, first (follow-up evolved wallet) | 448,708 | 0 | 22,000 | **470,708** |
| Billing no-primary-asset payee pull, same-block repeat (follow-up evolved wallet) | 323,461 | 0 | 22,000 | **345,461** |
| Billing cheque pull | 515,998 | 52,800 | 22,000 | **485,198** |

The historical same-block repeat Billing payee pull is `116,198` gas cheaper than the matching-primary owner push. Subtracting the follow-up evolved-wallet no-primary Billing result from the earlier no-primary owner result gives `116,226`—only `28` gas different. That cross-scenario subtraction strongly corroborates the authorization delta but is not a cloned-state paired measurement. The two routes also have different entry points and potentially different authorization/product semantics, so this is evidence about authorization cost rather than a recommendation to replace every owner push with a Billing pull.

### 5.4 Cheque lifecycle

The historical cheque figures below use first-use cheque-accounting state. Owner creation and owner payment are separate sequential transactions with no block advance; the atomic row is an independent first-use scenario. Caller and manager state differ across the owner, Billing, agent-payment, and agent-atomic routes.

| Flow | Gross execution | Refund | Intrinsic | Receipt-like gas |
|---|---:|---:|---:|---:|
| Owner creates cheque, first-use accounting | 691,932 | 0 | 22,892 | **714,824** |
| Owner pays that existing cheque, following transaction | 633,290 | 52,800 | 22,268 | **602,758** |
| Agent pays existing cheque, first-use route | 772,637 | 52,800 | 23,552 | **743,389** |
| Agent atomic create-and-pay, independent first-use scenario | 1,171,099 | 218,900 | 23,400 | **975,599** |

Lifecycle totals:

| Lifecycle | Total gas |
|---|---:|
| Historical first-use owner create + owner pay | **1,317,582** |
| Historical first-use owner create + Billing pull | **1,200,022** |
| Historical first-use owner create + agent pay | **1,458,213** |
| Independent first-use atomic agent create-and-pay | **975,599** |

Atomic savings:

- Versus owner create + owner pay: `341,983` gas (`26.0%`).
- Versus owner create + Billing pull: `224,423` gas (`18.7%`).
- Versus owner create + agent pay: `482,614` gas (`33.1%`).

These are valid measured **product-route** comparisons, not identical-call-path comparisons. Every two-transaction route begins with owner-driven creation and then uses an owner, Billing, or agent payment entry, while the atomic route is agent-mediated and includes its own manager authorization/accounting. The percentages therefore do not isolate the saving caused solely by atomicity. Use the atomic route only when immediate execution, agent authorization, cheque thresholds/delays, and non-pullable semantics match the intended product action.

A follow-up mature cheque-accounting-class observation measured owner create + owner pay at `606,717 + 468,790 = 1,075,507` and agent atomic create-and-pay at `691,384`, a `384,123`-gas (`35.7%`) product-route saving. Those calls still differ by caller path and ran sequentially rather than from cloned state, so this is corroboration rather than a mechanism-isolated estimate.

### 5.5 Batching

| Flow | Gross execution | Refund | Intrinsic | Receipt-like gas |
|---|---:|---:|---:|---:|
| First generic-agent transfer as separate transaction | 518,348 | 0 | 23,388 | **541,736** |
| Second generic-agent transfer as separate transaction | 393,154 | 0 | 23,388 | **416,542** |
| Two generic-agent transfers in one existing batch | 645,153 | 0 | 28,856 | **674,009** |

Two separate transactions cost `958,278` gas. The batch costs `674,009`, saving:

```text
958,278 - 674,009 = 284,269 gas
284,269 / 958,278 = 29.7%
```

This is an immediately actionable product-routing optimization when atomicity and user intent permit batching.

A follow-up sequential mature-manager-state observation measured the separate calls at `433,654 + 416,554 = 850,208` and the batch at `565,915`, saving `284,293` gas (`33.4%`). The recipient storage classes are matched—fresh recipient then repeat recipient—but the calls run forward-only rather than from cloned exact state. Treat the result as mature-state corroboration, not a universal state-independent percentage.

---

## 6. Same-Block Repeat Owner Transfer Hotspot Attribution

The same-block repeat owner-to-whitelist ERC20 flow has `351,839` gross contract execution gas. Exclusive call-tree attribution identified the following major components:

| Component | Approximate gas | Share of 351,839 execution |
|---|---:|---:|
| Global manager settings getter | 58,201 | 16.5% |
| Signer-specific manager settings getter | 56,625 | 16.1% |
| `UserWallet.transferFunds` exclusive work | 41,421 | 11.8% |
| Two `VaultRegistry.isBasicEarnVault` calls | 26,886 | 7.6% |
| `ActionDataProvider` exclusive work | 26,541 | 7.5% |
| Sixteen UndyHq address lookups | 21,808 | 6.2% |
| Manager period data getter | 15,488 | 4.4% |
| Ledger points getter | 10,933 | 3.1% |
| Repeat ERC20 transfer | 10,194 | 2.9% |
| Ledger vault-token getter | 9,384 | 2.7% |
| Ledger points setter | 8,965 | 2.5% |
| Appraiser exclusive work | 7,955 | 2.3% |

These rows are exclusive call-tree components, but they should still be treated as diagnostic attribution rather than an additive optimization budget. Removing one call can change warmth, memory expansion, ABI encoding, and downstream exclusive costs.

The most important comparison is:

```text
Current three-getter owner burden: 130,314 gas
Actual repeat ERC20 transfer:       10,194 gas
Ratio:                               12.8×
```

For the measured owner-not-manager shortcut case, the wallet spends almost thirteen times more gas on the current manager-data retrievals than it spends moving the token, even though pre-authorization needs only the scalar `canOwnerManage` from those retrieved structures.

---

## 7. Root-Cause Analysis

### 7.1 Eager owner authorization arguments

[`ActionDataProvider.checkSignerPermissionsAndGetBundle`](../../contracts/core/userWallet/ActionDataProvider.vy#L139) retrieves manager period data, full manager settings, and full global manager settings before calling Sentinel.

[`Sentinel`](../../contracts/core/walletBackpack/Sentinel.vy#L102) then checks whether the signer is the wallet owner and whether owner management is enabled. If true, it returns without using the manager-specific structures.

This is a language/call-architecture issue rather than a subtle opcode issue: external-call arguments are evaluated before the callee can short-circuit. For an owner-not-manager with `canOwnerManage = true`, pre-authorization needs the scalar `canOwnerManage`, but it does not need the rest of the global settings structure or either signer-specific manager structure.

The remedy must move the owner branch before those getters. Merely adding another early return inside Sentinel will not help.

The owner-manager edge changes the achievable saving. When `canOwnerManage = false`, the full manager structures remain necessary for manager fallback. When it is true, pre-authorization can skip them, but manager post-accounting still needs the same structures; removing the pre-read makes many later settings/data slots cold rather than warm. The ordinary owner-not-manager estimate must therefore not be generalized to owner-managers without a dedicated benchmark.

### 7.2 Oversized generic action bundle

The generic action bundle includes approximately 20 fields and resolves registry addresses such as LegoBook, Ledger, MissionControl, Hatchery, LootDistributor, Appraiser, Billing, and VaultRegistry.

Normal transfers do not need every one of these fields. The bundle also causes large ABI encoding/decoding and memory work and contributes to repeated UndyHq lookups.

A transfer-specific bundle can be materially smaller and can avoid unrelated swap, Lego, liquidity, and deployment configuration.

### 7.3 Storage-heavy payee data

The payee flow retrieves and rewrites a full eight-field `PayeeData` structure. In the measured matching-primary-asset case, first use initializes all eight accounting fields and produces the `235,890`-gas first validation/write cost. Same-block repeat updates are cheaper but remain approximately `93,490` gas. Without a matching primary asset, the two unit fields remain zero; the maintained end-to-end owner benchmark is `40,114` gas cheaper on first use and `5,914` cheaper on same-block repeat, but no separate component trace was captured for that variant.

Some values are needed for active on-chain limits. Others, such as lifetime transaction counters used only for analytics, are candidates for events or an indexer.

### 7.4 Storage-heavy manager data

Manager post-accounting reads settings and period state again and rewrites a seven-field `ManagerData` structure.

The first update costs approximately `132,253` gas because it initializes multiple storage words. Same-block repeat updates cost approximately `24,053` gas.

### 7.5 Cheque persistence and double validation

Cheque creation persists a large cheque structure and updates cheque creation/accounting state. Payment later retrieves, validates, updates global paid-period data, decrements active count, and clears the stored cheque.

For delayed cheques, persistence is necessary. For a cheque created and paid atomically, storing a complete cheque is avoidable in principle.

The current atomic flow receives substantial same-transaction storage refunds, so its net cost is much lower than gross execution. Nevertheless, it still pays for multiple authorization, validation, pricing, accounting, and cross-contract passes. A dedicated instant path should target those duplicate passes, not merely the storage clear.

### 7.6 Duplicate pricing and vault detection

The wallet prices the outgoing amount before the transfer at [`UserWallet.vy`](../../contracts/core/userWallet/UserWallet.vy#L165). For a partial transfer, it later reads and prices the remaining balance at [`UserWallet.vy`](../../contracts/core/userWallet/UserWallet.vy#L1166).

Both Appraiser paths repeat configuration, authorization, oracle, and vault-classification work.

`VaultRegistry.isBasicEarnVault` loads a complete vault configuration to answer a Boolean question. The two calls consume `26,886` gas even for an ordinary non-vault ERC20.

### 7.7 Synchronous deposit-points accounting

Every non-eject transfer calls [`LootDistributor.updateDepositPointsWithNewValue`](../../contracts/core/LootDistributor.vy#L641), which reads user/global point structures and writes through Ledger.

This keeps rewards continuously synchronized but places reward-accounting cost on every payment, including the high-frequency transfer path Hightop sponsors.

### 7.8 Billing duplication

[`Billing._pullPayment`](../../contracts/core/Billing.vy#L207) checks ERC20 balance at least twice in the no-withdrawal case and then always attempts wallet asset deregistration at [`Billing.vy`](../../contracts/core/Billing.vy#L236), even when the transfer is partial.

The wallet subsequently performs its own balance and asset-accounting work. This is a smaller hotspot than owner authorization or payee storage, but it is a clear cleanup opportunity.

### 7.9 Untracked-asset churn

The fully fresh untracked-native scenario costs `506,800`, compared with `388,286` for a tracked native transfer to the same kind of empty recipient. That `118,514`-gas end-to-end difference includes asset registration and fresh wallet/points initialization. A follow-up evolved-wallet scenario that already tracked another ERC20 measured `469,821` for untracked native, `81,535` above the same `388,286` tracked-native receipt. The follow-up calls are forward-only and not cloned-state comparators, so `81,535–118,514` is a range of observed end-to-end deltas across two initialization states, not an isolated measurement of registration alone. Comparing the fully fresh untracked case against the `363,286` tracked transfer to an existing recipient produces a `143,514`-gas difference, but `25,000` of that is the unrelated native new-account charge.

Wallets that frequently empty, deregister, refill, and transfer a canonical payment asset can repeatedly pay this churn. A limited sticky-registration policy for ETH and selected payment tokens may be beneficial, but it increases the number of zero-balance assets retained for enumeration and accounting.

### 7.10 Agent signature duplication

The signed agent path hashes action data, performs `ecrecover`, validates and writes a nonce, emits nonce-related state/events, and carries larger outer calldata before reaching AgentWrapper and the wallet.

If the external sponsored account or session-key layer proves exactly the same authority and scope, Hightop may be paying for duplicate authentication. Removing a layer changes the security boundary and should only follow an explicit threat-model review.

---

## 8. Detailed Recommendations

The savings ranges below are measured where explicitly stated and modeled otherwise. They overlap.

### R1. Add an owner-specific authorization fast path

**Priority:** Highest  
**Affected flows:** Owner actions with `canOwnerManage = true`; owner-not-manager is the primary saving case\
**Expected saving:** Modeled `120,000–150,000` gas for owner-not-manager; both owner-manager branches are unmeasured\
**Evidence quality:** `130,314` current getter burden measured; replacement net saving not yet measured  
**Risk:** Medium-high; authorization and accounting critical\
**Existing wallets:** Requires migration to a new wallet/config pair; deploying only a new Config or ActionDataProvider is insufficient

The flow should conceptually become:

```text
1. Build ActionData as today, including independent ad.isManager.
2. Preserve the Billing early return.
3. Preserve locked-signer validation.
4. Determine isOwner = signer == ad.walletOwner.
5. If isOwner and a dedicated scalar canOwnerManage getter returns true:
   a. Return without loading managerPeriodData, managerSettings, or the
      full globalManagerSettings for the pre-action Sentinel call.
   b. Do not clear or override ad.isManager.
6. Otherwise execute the complete existing manager path.
   This includes owner-manager fallback when canOwnerManage is false.
7. Leave UserWallet post-action dispatch unchanged: ad.isManager still
   controls manager post-limits and counter updates.
```

`UserWalletConfig` currently exposes the complete public `globalManagerSettings` structure rather than a dedicated scalar external getter. The fast path therefore needs a one-slot `canOwnerManage` view API in a new Config template, or an equivalently compact API whose gas is measured.

The relevant owner cases are:

- Owner-not-manager + `canOwnerManage = true`: owner pre-shortcut; no manager post-accounting.
- Owner-manager + `canOwnerManage = true`: owner pre-shortcut; `ad.isManager` remains true; manager post-limits and counter updates still run.
- Owner-manager + `canOwnerManage = false`: full manager pre-authorization and post-accounting remain required. An owner-not-manager in this setting is rejected.

The `120,000–150,000` estimate applies only to the first case. In the second, post-accounting still reads the manager structures and many settings/data slots become cold if the pre-read is removed, so the net saving will be materially lower and must be measured. The third case receives no owner-fast-path saving and may incur a small scalar/dispatch overhead.

Required invariants:

- Frozen-wallet behavior remains unchanged.
- Ejection-mode behavior remains unchanged.
- Non-Billing locked signers remain blocked; Billing's existing early-return ordering remains unchanged.
- The `canOwnerManage` owner shortcut and owner-manager fallback decisions remain identical.
- Owner/manager classification remains independent.
- Owner-manager fallback when `canOwnerManage = false` remains unchanged.
- Owner-manager post-accounting when `canOwnerManage = true` remains unchanged.
- Specific/global zero-price and USD-cap post reverts, manager counter writes, period resets, equality-at-cap behavior, and atomic rollback remain identical.
- Pre-only activation, permission, allowlist, count-limit, and cooldown behavior remains identical for every owner/manager combination.
- Recipient whitelist/payee/cheque validation still runs.
- Billing's current special handling remains unchanged.

This should be validated through differential tests that execute the old and new authorization logic over the same generated owner/manager/settings states. Construct the owner-manager state by adding a manager and then transferring ownership to that address. Tests must cover `canOwnerManage` true and false, restrictive/expired manager settings, disabled transfer permission, disallowed assets/payees, cooldown/count caps, specific and global `failOnZeroPrice`, per-transaction/period/lifetime USD caps, equality at each cap, successful counter deltas, first-period initialization, period rollover, and full rollback when a post-check rejects.

If Hightop instead intends owner status to suppress all manager post-accounting, specify and audit that as a separate protocol behavior change. It must not be folded silently into this gas optimization.

### R2. Make basic-vault classification O(1)

**Priority:** Very high  
**Affected flows:** Nearly every priced transfer  
**Expected saving:** Modeled approximately `20,000–25,000` gas per cold transfer  
**Evidence quality:** Current two-call cost of `26,886` measured; replacement mapping cost not yet measured  
**Risk:** Low to medium  
**Existing wallets:** Potentially, but only through a governed Appraiser/VaultRegistry upgrade with complete state migration and rotation

Maintain a dedicated mapping or bitset such as `isBasicEarnVault[address]`. Update it on every vault add, update, disable, and removal path.

For existing wallets, this is not a trivial registry swap. A new VaultRegistry requires complete migration of canonical vault configuration plus governed address rotation, or the fast flag must live behind a newly rotated Appraiser that preserves all existing pricing semantics.

The optimization is safe only if the Boolean is an invariant of the canonical vault configuration. Tests must prove that no registry mutation path can leave the Boolean stale.

### R3. Add a transfer-specific authorization API and compact bundle

**Priority:** High  
**Affected flows:** All direct wallet transfers  
**Expected saving:** Approximately `20,000–100,000` gas  
**Evidence quality:** Root-cause measurement plus modeled redesign  
**Risk:** Medium  
**Existing wallets:** Usually new templates/migration

The transfer bundle should contain only fields required for:

- Wallet and signer identity.
- Frozen/eject/locked state.
- Owner or manager classification.
- Transfer-specific permissions and counters.
- Recipient classification.
- Ledger/Appraiser/Loot/Billing addresses used by the transfer.

It should not retrieve unrelated Lego, swap, liquidity, or deployment data.

For manager allowed assets and payees, maintain O(1) membership mappings. Arrays may remain for enumeration, UI reads, and migration, but validation should not scan them.

### R4. Use existing batching immediately and design a wallet-native batch

**Priority:** High for multi-payment workloads  
**Affected flows:** Agent and future owner/manager multi-payment actions  
**Measured product-route saving:** Historical first-manager-use `284,269` gas (`29.7%`); sequential mature-manager-state follow-up `284,293` gas (`33.4%`) for two generic-agent transfers\
**Modeled future saving:** Approximately `100,000–250,000` for each additional transfer relative to the current separate-call architecture  
**Risk:** Medium to high for a new wallet-native batch

Both measured percentages are local direct-transaction-equivalent results, not reductions in complete production sponsored cost. The mature follow-up matches recipient storage classes but is forward-only rather than cloned-state. The modeled per-additional-transfer range will shrink after other optimizations such as the owner fast path, compact bundle, and points redesign; it is not additive to them.

Immediate action:

- Route two or more compatible agent payments through the existing batch entry point.

Future wallet-native batch:

- Authenticate once.
- Group payments by asset.
- Price each asset once where safe.
- Update wallet asset data once per asset.
- Update deposit points once after final wallet value is known.

The batch must preserve per-payment semantics:

- Recipient/payee/cheque validation.
- Per-transaction count limits.
- Manager and payee cooldowns.
- Ordering and atomicity.
- Exact event/indexer behavior or an explicitly versioned replacement.

### R5. Use the atomic cheque route now and build a non-persistent instant cheque

**Priority:** High only when cheque semantics are already required for the one-off payment  
**Affected flows:** Cheques intended to be created and paid immediately  
**Measured historical first-use product-route saving:** `224,423–482,614` gas depending on comparison path; sequential mature-accounting follow-up `384,123` gas (`35.7%`); mechanism-isolated saving unmeasured\
**Modeled dedicated-path saving:** Approximately `200,000–400,000` additional gas  
**Risk:** Medium product/security-semantic risk for selecting the existing route; high for a new dedicated path  
**Existing wallets:** Existing atomic route yes; dedicated new route generally requires new contracts/templates

Immediate action:

- Use `AgentWrapper.createAndPayCheque` only when cheque semantics are already required, the cheque is intended to execute immediately, and the agent/product authorization model is appropriate. Do not route an ordinary whitelist or payee payment through a cheque merely to pursue the lifecycle percentage saving.
- Treat the measured percentages as comparisons between product routes with different owner/Billing/agent authorization envelopes, not as a pure estimate of storage/atomicity savings.

Future action:

- Validate creation and payment rules in one function.
- Price once.
- Update every required creation and payment counter/last-block field once, with the same shared-period-reset semantics as the current two-stage lifecycle.
- Do not persist a full cheque that is never intended to survive the transaction.

The new path must preserve:

- Allowed asset rules.
- Manager cheque permissions.
- Allowed-payee rules where applicable.
- Creation-period limits.
- Paid-period limits.
- Both creation and paid counters and last-block fields.
- Shared creation/payment period-reset behavior.
- Creation and payment cooldowns.
- `instantUsdThreshold` eligibility and expensive-cheque delay behavior.
- Maximum-active-cheque semantics or an explicitly proven atomic equivalent.
- Nonzero price/value validation.
- Exact amount and asset.
- Locked/frozen/eject behavior.
- Replay prevention.

Delayed, locked-until-later, or expiring cheques should retain the persistent lifecycle.

An alternative future architecture is an EIP-712 one-time payment permit with a bitmap nonce. That can eliminate the persistent cheque but materially changes the authorization and recovery model and should be treated as a separate security project.

### R6. Pack manager, payee, and cheque period data

**Priority:** High for first-use payees/managers and cheque-heavy wallets  
**Affected flows:** Manager, payee, and cheque accounting  
**Expected saving:** Approximately `10,000–120,000`, depending on path and first/repeat state  
**Risk:** High storage-layout and overflow risk  
**Existing wallets:** Requires migration or new template

Current logical layouts include:

- `ManagerData`: seven fields/slots.
- `PayeeData`: eight fields/slots.
- `ChequeData`: eleven fields/slots.

Vyper does not automatically pack independent struct fields into a shared storage word. Use explicit packed words with documented and checked bounds.

Candidate widths:

- Block numbers and transaction counts: typically `uint64` with explicit maximums.
- USD and unit totals: checked `uint128` or `uint192`, selected from real protocol maximums rather than assumption.

Candidates for removal from on-chain storage include lifetime counters used only for analytics, such as `totalNumTxs` and cumulative cheque statistics that do not participate in validation. Emit events and reconstruct them in an indexer instead.

Before removal, run a complete read-site search to prove that no contract, migration, governance, or UI-critical view depends on the field.

### R7. Pack or redesign deposit-points accounting

**Priority:** Medium to high because it affects every transfer  
**Affected flows:** All non-eject transfers  
**Expected saving:** `10,000–25,000` conservatively; `25,000–70,000` with redesign  
**Risk:** High for reward correctness

Conservative changes:

- Pack `usdValue` with `lastUpdate` where numeric bounds allow.
- Avoid provable no-op same-block writes.
- Avoid rereading values already available from wallet post-accounting.

Aggressive changes:

- Use a global reward accumulator with lazy per-user settlement.
- Settle points only on balance-changing actions that actually cross an accrual boundary.
- Move some accounting to events plus Merkle or indexed claims.

Any redesign requires invariants around time, global totals, user entry/exit, rounding, and multiple actions in one block.

### R8. Combine outgoing and remaining-balance pricing

**Priority:** Medium to high  
**Affected flows:** Partial transfers of standard assets  
**Expected saving:** Approximately `10,000–30,000` gas  
**Risk:** Medium

For explicitly supported standard, non-rebasing, non-fee-on-transfer assets, a transfer-specific Appraiser endpoint can return:

- USD value of the transfer.
- USD value of the expected remaining balance.
- Yield classification.

from a single price/configuration observation.

Do not infer the post-transfer balance for arbitrary tokens. Retain an actual second `balanceOf` and conservative pricing path for:

- Fee-on-transfer tokens.
- Rebasing tokens.
- Yield wrappers with nonlinear price-per-share behavior.
- Tokens with callbacks or nonstandard balance behavior.

### R9. Remove duplicate Billing balance and deregistration work

**Priority:** Medium  
**Affected flows:** Billing payee and cheque pulls  
**Expected saving:** Approximately `5,000–15,000` gas  
**Risk:** Low to medium

For explicitly supported standard tokens:

- Cache the pre-transfer balance.
- Avoid the second Billing-level balance read when no withdrawal occurred.
- Do not call deregistration for a known partial payment.
- Pass known balance information to the wallet only if doing so cannot be forged and does not weaken token compatibility.

### R10. Consider sticky tracking for canonical payment assets

**Priority:** Data-dependent  
**Affected flows:** Wallets that frequently empty and refill ETH or a principal stablecoin  
**Observed end-to-end scenario delta:** Untracked native was `81,535–118,514` gas more expensive than tracked native with an empty recipient across forward-only evolved and fully fresh initialization states; isolated registration cost unmeasured\
**Risk:** Medium product/accounting tradeoff

Possible approaches:

- Never deregister native ETH from the wallet asset list.
- Retain selected canonical payment assets at zero balance.
- Register funded payment assets during the inbound/deposit flow rather than during the outbound payment.

Tradeoffs:

- Larger zero-balance asset lists.
- More expensive enumeration or full-wallet updates.
- Potential UI confusion unless zero balances are filtered.
- Need to keep wallet total USD and points at zero correctly.

This should be driven by production data showing how often users empty and later refill the same asset.

### R11. Consolidate duplicate agent authentication only after threat modeling

**Priority:** Medium  
**Affected flows:** Signed agent actions inside an already authenticated sponsored account/session  
**Expected saving:** Approximately `10,000–30,000` execution gas plus outer calldata bytes  
**Risk:** High security-boundary risk

If the ERC-4337 account or session key authenticates the same signer, wallet, action type, parameters, expiry, and nonce scope, the inner agent signature may duplicate work.

Before consolidation, document:

- Which key compromise each layer protects against.
- Domain separation.
- Replay domains and chain IDs.
- Session scope.
- Nonce ownership and cancellation.
- Server/agent key compromise behavior.

If both layers remain necessary, reduce encoding cost with fixed compact signature fields such as `(r, vs)` and packed nonce/expiry values rather than dynamic bytes where ABI compatibility permits. See [EIP-2098](https://eips.ethereum.org/EIPS/eip-2098).

### R12. Apply micro-optimizations only after structural work

**Priority:** Low  
**Expected saving:** Usually hundreds to a few thousand gas each

Candidates:

- Cache balances for explicitly supported standard assets.
- Avoid the third `balanceOf` call during known-zero deregistration.
- Reorder `_deregisterAsset` guards so cheap index/count checks happen before external token calls.
- Use the shortest generated Vyper `transferFunds` overload rather than sending default Boolean arguments explicitly.
- Consider a smaller transfer-specific event only if indexer compatibility can be versioned safely.

The existing `WalletAction` event and individual ABI/memory operations are not large enough to outrank authorization, storage, pricing, batching, or points work.

---

## 9. Cheque Refund Interpretation

Cheque deletion deserves careful interpretation because gross execution and paid gas differ substantially.

Measured refunds were:

- Existing cheque payment: `52,800` gas.
- First-use atomic create-and-pay reported in Section 5: `218,900` gas.

Both were below the 20% refund cap for their historical measured pre-refund gas, so the full refund was usable in those scenarios. A follow-up mature atomic observation had the same raw `218,900` refund counter but only `172,846` applied because the 20% cap bound at its lower gross execution. Refund usability is therefore state- and gross-gas-dependent; do not subtract the raw counter without applying the cap.

Consequences:

- Clearing the full cheque is not as expensive in net paid gas as gross `SSTORE` attribution suggests.
- Replacing a full clear with only `active = False` is not a first-tier fee optimization.
- A tombstone retains stale data and requires every reader and future recreation path to overwrite or ignore it correctly.
- The best instant-cheque optimization is to eliminate duplicate authorization/pricing/accounting and avoid persistence entirely, not merely to avoid clearing slots.

For delayed cheques, full clearing remains defensible for state hygiene and replay safety unless a separately audited versioned/tombstone design proves a material net benefit.

---

## 10. Native Full-Balance Correctness Finding

### Finding

Execution reaches the native send, then later reverts, atomically rolling back the transfer.

The same failure applies when `MAX_UINT256` is passed and clamped to the wallet's entire native balance.

### Root cause

After the transfer leaves a zero balance, post-accounting enters the zero-balance branch at [`UserWallet.vy`](../../contracts/core/userWallet/UserWallet.vy#L1149) and calls `_deregisterAsset`.

`_deregisterAsset` first executes an ERC20 `balanceOf` call at [`UserWallet.vy`](../../contracts/core/userWallet/UserWallet.vy#L1206), even when `_asset` is the native sentinel address. The sentinel has no ERC20 bytecode, so the empty return data fails ABI decoding before later early-return guards can run.

The existing native transfer test at [`tests/core/userWallet/test_user_wallet.py`](../../tests/core/userWallet/test_user_wallet.py#L129) transfers only half the native balance and does not enter this path.

### Recommended fix

Either:

1. Branch on the native sentinel and use `self.balance`, or
2. Pass the already-known zero post-transfer balance into an internal deregistration function and avoid another external balance query.

Also move cheap `numAssets`/index guards before any token external call where invariant ordering permits.

Because `UserWallet` instances are immutable blueprint deployments, this fix cannot patch an existing wallet in place. Existing wallets require migration to a corrected wallet/config pair. Until migration is available, the operational mitigation is to prevent exact-full native payments and leave a small native balance in the wallet; this is a temporary UX guard, not a contract fix.

### Required tests

- Exact explicit native balance transfer.
- `MAX_UINT256` clamped to the full native balance.
- Full native transfer when the asset is registered.
- Full native transfer when asset/accounting state is unusual but valid.
- Partial native transfer continues to retain the asset.
- Full ERC20 behavior remains unchanged.

---

## 11. Base and Sponsorship Cost Model

### 11.1 Base has two major transaction-cost components

Base documents every transaction as having:

- An L2 execution fee.
- An L1 security/data fee used to publish transaction data.

Base notes that the L1 component is typically larger. See [Base Network Fees](https://docs.base.org/base-chain/network-information/network-fees).

The wallet optimizations in this report primarily reduce **L2 execution gas**:

- Storage reads and writes.
- Cross-contract calls.
- Authorization.
- Pricing.
- Token execution.
- Logs.
- Points accounting.

Large internal structs and returndata do not directly add L1 calldata bytes. Only the outer transaction or UserOperation encoding is published as input data.

Optimizations such as compact signatures, shorter function overloads, smaller `callData`, and smaller `paymasterAndData` can reduce the L1-data component as well as pre-verification gas.

### 11.2 Current minimum Base fee reference

As of this report date, Base documents a Base Mainnet minimum L2 base fee of `0.005 gwei`, subject to periodic adjustment.

At that illustrative floor:

```text
L2 execution cost in ETH = gas_used × 0.005 gwei / 1e9
```

For an illustration using the full current three-getter burden—not a claim that every unit will be eliminated after the replacement scalar read:

```text
130,314 gas × 0.005 gwei = 651.57 gwei
                              = 0.00000065157 ETH
```

At the Base documentation's illustrative `$2,000/ETH`, this is approximately:

- `$0.00130` per payment.
- `$1,303` per million payments.

This is L2 execution only. Actual savings rise when the L2 gas price is above the floor and do not include any L1-data or operator changes.

### 11.3 Measuring Base L1 fees

Base exposes the GasPriceOracle predeploy at:

```text
0x420000000000000000000000000000000000000F
```

Relevant methods include:

- `getL1Fee(bytes)` for a fully serialized transaction.
- `getL1FeeUpperBound(uint256)` for a size-based upper bound.

See the [Base fee documentation](https://docs.base.org/base-chain/network-information/network-fees) and [Base execution-fee specification](https://docs.base.org/base-chain/specs/protocol/execution/index).

### 11.4 The external sponsorship layer is not in this repository

The contract named [`Paymaster.vy`](../../contracts/core/walletBackpack/Paymaster.vy#L10) is the wallet's payee-management module. It is not the external ERC-4337 gas paymaster.

The full Hightop-sponsored cost may additionally contain:

- Account `validateUserOp` execution.
- Paymaster validation.
- Paymaster `postOp`.
- `preVerificationGas`.
- EntryPoint overhead.
- Bundler/operator surcharge.
- Outer UserOperation signature and calldata.
- Base L1-data fee.

These components require production UserOperation receipts and traces. See [ERC-4337](https://eips.ethereum.org/EIPS/eip-4337).

### 11.5 Recommended business KPI

Track:

```text
sponsored_cost =
    execution reimbursement
  + allocated Base L1 transaction fee
  + bundler/provider surcharge
```

If the provider invoice already bundles the Base L1 fee or surcharge into a single charged amount, use the invoiced components instead and do not double count them. ERC-4337 `actualGasCost`/reimbursement fields and provider billing conventions must be reconciled against the bundler's outer Base transaction.

Break it into:

```text
L2 wallet execution
+ L2 account/paymaster/EntryPoint execution
+ L1 data/security fee
+ operator surcharge
```

Report median, p90/p95, and total dollars per million payments for every major flow class.

---

## 12. Production Profiling Plan

Before selecting storage migrations or security-model changes, collect at least 30–100 production Base samples in each high-volume segment.

### 12.1 Segmentation matrix

Segment by:

- Signer: owner, manager, generic agent, signed agent.
- Asset: native, primary stablecoin, other ERC20, yield asset.
- Balance behavior: partial, exact full balance.
- Recipient: whitelist, registered payee, cheque.
- State: first payment, initialized/repeat payment.
- Entry path: direct wallet, Billing, agent, batch.
- Batch size: 1, 2, 3, 5, and practical upper percentiles.
- Recipient account state: existing versus empty/new native recipient.

### 12.2 Per-transaction fields

Collect:

- Transaction hash and block.
- Wallet version/template.
- Asset and recipient class without recording sensitive user metadata.
- `actualGasUsed`.
- `actualGasCost`.
- Effective L2 gas price.
- L1 fee.
- Outer calldata/UserOperation byte length.
- Account-validation gas.
- Paymaster-validation gas.
- `postOp` gas.
- Wallet-call execution gas.
- Bundler/operator surcharge.
- Whether recipient/payee/manager period state was first-use or initialized.

### 12.3 Trace attribution

Attribute call-tree gas to:

- EntryPoint/account/paymaster.
- AgentSender and AgentWrapper.
- UserWallet.
- UserWalletConfig.
- ActionDataProvider.
- Sentinel.
- Appraiser and oracle/price infrastructure.
- VaultRegistry.
- ERC20/native transfer.
- LootDistributor and Ledger.
- Billing.

### 12.4 Reports

Produce:

- Median and p95 gas by flow.
- Median and p95 sponsored dollars by flow.
- L2 execution versus L1-data share.
- First-use versus repeat delta.
- Savings from actual batching behavior.
- Cost per one thousand and one million payments.
- Frequency-weighted optimization opportunity.

Prioritize engineering using:

```text
annual_savings_opportunity =
    flow_frequency
  × measured_per_flow_saving
  × effective_fee
```

This prevents a very large but rare cheque optimization from displacing a smaller per-call improvement on millions of owner whitelist transfers.

---

## 13. Proposed Implementation Roadmap

### Phase 0: Measurement and correctness

1. Fix exact-full native transfers.
2. Add transaction-boundary gas tests to CI.
3. Add production Base/UserOperation cost telemetry.
4. Establish median and p95 budgets for the current wallet version.

Suggested baseline CI scenarios:

- Owner whitelist ERC20 first/repeat/full.
- Owner payee first/repeat.
- Owner-manager created through ownership transfer, with `canOwnerManage` true/false, manager period initialized/uninitialized, and transfers under/equal to/over specific and global USD caps.
- Manager whitelist first/repeat.
- Generic and signed agent transfer.
- Billing payee first/repeat.
- Cheque create, pay, and atomic create/pay.
- Native partial/full and existing/new recipient.
- Two- and three-payment batches.

### Phase 1: High-return, bounded changes

1. O(1) `isBasicEarnVault` lookup.
2. Owner authorization fast path.
3. Remove Billing duplicate balance/deregistration work.
4. Route existing product actions through batch and atomic cheque paths.
5. Use the shortest calldata overloads.

Target for a same-block repeat owner-not-manager whitelist payment with `canOwnerManage = true` after the first two structural changes:

- Current measured scenario: `373,839` direct-transaction-equivalent gas.
- Engineering target: approximately `220,000–250,000` gas.

This target is directional and must be remeasured because the savings overlap through call warmth and ABI work. It does not apply to an owner-manager: with `canOwnerManage = true`, manager post-accounting remains and many settings/data slots become cold when the pre-read is removed; with `canOwnerManage = false`, the R1 fast path does not apply. Measure both owner-manager branches independently before assigning a target.

### Phase 2: New transfer architecture

1. Transfer-specific authorization bundle.
2. O(1) manager asset/payee membership.
3. Combined standard-token pricing/accounting.
4. Wallet-native batch.
5. Dedicated instant-cheque path.

Suggested long-term engineering target for a same-block repeat owner-not-manager, `canOwnerManage = true`, owner-to-whitelist standard ERC20 payment:

- Less than `200,000` direct-transaction-equivalent gas before the external AA/sponsorship envelope.

This is a target, not a measured forecast. Owner-manager targets remain unmeasured.

### Phase 3: Storage and rewards redesign

1. Packed manager/payee/cheque period data.
2. Removal of analytics-only storage writes.
3. Packed or lazy deposit-points accounting.
4. Wallet/config migration tooling and versioned ABI support.

This phase has the highest migration and correctness risk and should follow production-frequency data.

---

## 14. Security and Correctness Test Gates

Every authorization or accounting optimization must preserve the following behavior.

### 14.1 Owner and signer invariants

- Frozen wallet cannot perform prohibited transfers.
- Ejection mode remains authoritative.
- Non-Billing locked signers remain blocked; Billing's existing early-return ordering remains unchanged.
- The `canOwnerManage` owner shortcut and owner-manager fallback decisions remain identical.
- Owner cannot bypass recipient classification or limits.
- Billing bypass applies only to the registered Billing address.

For a normal, non-special transfer by a non-Billing signer, the owner/manager truth table must remain:

| Owner | Manager | `canOwnerManage` | Pre-action result | Manager post-accounting |
|---|---|---|---|---|
| Yes | No | True | Owner shortcut succeeds | None |
| Yes | No | False | Rejects | None |
| Yes | Yes | True | Owner shortcut succeeds; manager pre-rules bypassed | Post limits and counters run |
| Yes | Yes | False | Complete manager path | Runs after successful pre-check |
| No | Yes | Either | Complete manager path | Runs after successful pre-check |
| No | No | Either | Rejects | None |

Construct the owner-manager rows by adding a manager first and then transferring ownership to that address; adding the current owner directly as manager is intentionally blocked. For the owner-manager/`canOwnerManage = true` row, restrictive or expired manager settings, disabled transfer permission, disallowed assets/payees, count caps, and cooldowns remain bypassed during pre-action. Specific and global zero-price/per-transaction/per-period/lifetime USD limits remain enforced post-action; rejection must roll back the token/native transfer and every recipient/manager write. Equality at a USD cap succeeds because the current comparison uses `>`, not `>=`. Successful actions must preserve manager counter deltas and period initialization/reset behavior. A non-swap does not increment `numSwapsInPeriod`, although period rollover may reset it to zero.

### 14.2 Manager invariants

- Manager activation timing.
- Allowed assets.
- Allowed payees.
- Transfer permission.
- Cooldown.
- Per-period transaction count.
- Per-transaction, per-period, and lifetime USD limits applicable to manager transfers.
- Global manager limits.
- Correct behavior at period boundaries.

### 14.3 Payee invariants

- Activation block.
- Allowed asset.
- Cooldown/count limits.
- Specific and global USD limits.
- Unit limits.
- First-payment and period-reset behavior.

### 14.4 Cheque invariants

- Active status.
- Unlock and expiry.
- Exact asset and amount.
- Allowed asset.
- Maximum value and nonzero price.
- Manager create/pay permissions.
- Creation and paid-period limits.
- Single-use/replay protection.
- Cancellation behavior.

### 14.5 Token and pricing invariants

- Standard ERC20.
- Proxy stablecoin used in production.
- Native asset.
- Full-balance transfer.
- Yield asset.
- Rebasing or fee-on-transfer tokens if supported.
- Zero, stale, or changing prices according to protocol policy.
- Correct post-transfer wallet USD total.

### 14.6 Points invariants

- Same-block multiple actions.
- Multiple-block accrual.
- Full asset exit.
- Asset re-entry.
- User/global point conservation or the protocol's intended relationship.
- Rounding bounds.

### 14.7 Differential testing

For new fast paths, generate identical action/settings state and execute both:

- The current generic logic in a reference deployment.
- The proposed specialized logic in a candidate deployment.

Assert identical success/revert decisions, returned values, storage deltas, and emitted semantic events.

---

## 15. Deployment and Bytecode Constraints

Wallet and Config instances are immutable blueprint deployments through [`Hatchery`](../../contracts/core/Hatchery.vy#L232). Each wallet stores its paired Config address during construction at [`UserWallet.vy`](../../contracts/core/userWallet/UserWallet.vy#L116), and `ACTION_DATA_PROVIDER` is captured immutably by each Config at [`UserWalletConfig.vy`](../../contracts/core/userWallet/UserWalletConfig.vy#L144).

Consequences:

- Owner fast paths affect future wallet/config templates; existing wallets require migration to a new paired wallet and Config. Deploying only a new Config or ActionDataProvider does not update the pinned pair.
- Packed storage requires a new layout and migration.
- Wallet-native batching requires a new wallet template.
- A dedicated instant-cheque entry point generally requires new templates/contracts.
- Global Appraiser/VaultRegistry changes are the best candidates to benefit existing wallets, but require governed rotation and complete preservation or migration of registry state; an Appraiser-side fast flag may reduce the migration surface.
- Billing changes may benefit existing wallets if the Billing address is globally resolved and can be rotated safely.

### 15.1 Runtime size

A separate Vyper `0.4.3` compiler probe, distinct from the transaction-boundary gas harness, measured the current code-size-optimized UserWallet runtime at `23,032` bytes against the EIP-170 limit of `24,576`, leaving `1,544` bytes of headroom.

For the comparison build, the temporary compiler input removed only `# pragma optimize codesize` and compiled with `vyper -p . -O gas -f bytecode_runtime`:

- UserWallet gas-optimized runtime: `23,883` bytes, leaving `693` bytes.
- AgentSenderGeneric gas-optimized runtime: `25,255` bytes, exceeding EIP-170 by `679` bytes.

These bytecode figures are compiler outputs, not transaction-gas artifacts.

Therefore:

- Do not begin with a blanket compiler switch from code-size to gas optimization.
- Prefer deleting/replacing generic machinery before adding new branches.
- Consider helper-contract placement for new logic, while accounting for extra external-call gas.
- Enforce bytecode-size gates in CI for every optimization PR.

See [EIP-170](https://eips.ethereum.org/EIPS/eip-170).

---

## 16. What Not to Optimize First

The following are real but secondary:

- `WalletAction` event fields.
- A single redundant registry getter.
- One or two memory copies.
- Individual Boolean ABI fields.
- Cheque tombstoning without redesigning the lifecycle.
- Blanket compiler optimization changes.

These do not approach the value of:

- Replacing the measured `130,314`-gas owner-not-manager pre-authorization retrieval burden with a scalar fast path.
- Avoiding large payee/manager first-use storage writes.
- Batching multiple transfers.
- Avoiding a two-transaction cheque lifecycle.
- Reducing duplicate pricing and points work.

Micro-optimization should happen after the call graph and state model are simplified.

---

## 17. Decision Summary

| Recommendation | Saving | Confidence | Security/migration risk | Recommended timing |
|---|---:|---|---|---|
| Owner authorization fast path | Owner-not-manager: 120k–150k modeled; 130,314 current burden measured. Owner-manager: unmeasured | High root-cause confidence; net and owner-manager paths unmeasured | Medium–high | First new-template optimization after differential tests |
| O(1) basic-vault flag | 20k–25k modeled; 26,886 current two-call cost measured | High root-cause confidence; net unmeasured | Low–medium | Near term; potentially existing wallets |
| Existing two-payment batch | Historical first-manager-use: 284,269 (29.7%); sequential mature-manager-state: 284,293 (33.4%) | High arithmetic confidence; mature run is forward-only | Low when atomic batch semantics fit | Use immediately when compatible |
| Existing atomic cheque | Historical first-use: 224k–483k; sequential mature-accounting follow-up: 384k, versus mixed-caller two-step product routes | High arithmetic confidence; mechanism-isolated saving unmeasured | Medium product/security-semantic risk | Use only when cheque and agent semantics already apply |
| Transfer-specific bundle | 20k–100k | Medium | Medium | New template |
| Wallet-native batch | 100k–250k per extra payment versus current architecture | Medium | Medium–high | New template |
| Non-persistent instant cheque | 200k–400k additional | Medium/low until built | High | Dedicated design/audit |
| Pack manager/payee/cheque data | 10k–120k | Medium | High | Storage-layout version |
| Pack/redesign points | 10k–70k | Medium | High | After reward-model analysis |
| Combine standard-token pricing | 10k–30k | Medium | Medium | New transfer path |
| Billing cleanup | 5k–15k | Medium | Low–medium | Near term |
| Agent auth/calldata consolidation | 10k–30k plus L1 bytes | Medium | High | Threat-model dependent |
| Sticky canonical assets | Part of an 81.5k–118.5k state-dependent observed delta | Low/medium | Medium | Production-data dependent |

---

## 18. Final Assessment

The current payment design is expensive because it is highly defensive and keeps many subsystems synchronized on every transfer. The core transfer instruction is already cheap; the surrounding general-purpose authorization and accounting architecture is the optimization opportunity.

For Hightop's sponsored high-frequency payments, the highest-confidence path is:

1. Remove unused owner-not-manager pre-authorization lookups while preserving owner-manager fallback and post-accounting.
2. Make vault classification O(1).
3. Batch compatible transfers.
4. Use atomic cheques for immediate one-off payments only when cheque and agent semantics match.
5. Create a compact transfer-specific path rather than continuing to optimize the generic action bundle around the edges.

Those changes attack the largest measured costs while preserving the existing payment categories. Storage packing, reward redesign, and non-persistent one-off authorization can provide further savings, but they carry substantially greater migration and security risk.

The existing opt-in transaction-boundary benchmark should be extended to cover the remaining routes and converted into a durable CI/measurement artifact. It should be paired with production Base UserOperation telemetry so Hightop optimizes the frequency-weighted sponsored dollar cost, not only local EVM gas in isolation.

---

## Appendix A. Key Code References

- [`UserWallet.transferFunds`](../../contracts/core/userWallet/UserWallet.vy#L144)
- [`UserWallet` pre-action tasks](../../contracts/core/userWallet/UserWallet.vy#L978)
- [`UserWallet` post-action tasks](../../contracts/core/userWallet/UserWallet.vy#L1020)
- [`UserWallet` post-transfer asset update](../../contracts/core/userWallet/UserWallet.vy#L1129)
- [`UserWallet._deregisterAsset`](../../contracts/core/userWallet/UserWallet.vy#L1204)
- [`ActionDataProvider.checkSignerPermissionsAndGetBundle`](../../contracts/core/userWallet/ActionDataProvider.vy#L139)
- [`Sentinel` owner authorization branch](../../contracts/core/walletBackpack/Sentinel.vy#L102)
- [`Sentinel` manager post-limit checks](../../contracts/core/walletBackpack/Sentinel.vy#L278)
- [`HighCommand.addManager` current-owner exclusion](../../contracts/core/walletBackpack/HighCommand.vy#L197)
- [`Ownership.changeOwnership`](../../contracts/modules/Ownership.vy#L104)
- [`UserWalletConfig` manager post-accounting](../../contracts/core/userWallet/UserWalletConfig.vy#L426)
- [`UserWalletConfig` recipient/payee validation](../../contracts/core/userWallet/UserWalletConfig.vy#L475)
- [`UserWalletConfig` cheque validation](../../contracts/core/userWallet/UserWalletConfig.vy#L524)
- [`UserWalletConfig` cheque creation](../../contracts/core/userWallet/UserWalletConfig.vy#L800)
- [`Appraiser` price update path](../../contracts/core/Appraiser.vy#L271)
- [`VaultRegistry.isBasicEarnVault`](../../contracts/registries/VaultRegistry.vy#L211)
- [`LootDistributor` deposit-points update](../../contracts/core/LootDistributor.vy#L641)
- [`Ledger` points write](../../contracts/data/Ledger.vy#L135)
- [`Billing._pullPayment`](../../contracts/core/Billing.vy#L207)
- [`AgentWrapper.transferFunds`](../../contracts/core/agent/AgentWrapper.vy#L103)
- [`AgentWrapper.createAndPayCheque`](../../contracts/core/agent/AgentWrapper.vy#L117)
- [`AgentSenderGeneric.transferFunds`](../../contracts/core/agent/AgentSenderGeneric.vy#L93)
- [`Hatchery` blueprint deployment](../../contracts/core/Hatchery.vy#L232)

## Appendix B. External References

- [Base Network Fees](https://docs.base.org/base-chain/network-information/network-fees)
- [Base L2 Execution and Fee Specification](https://docs.base.org/base-chain/specs/protocol/execution/index)
- [EIP-170: Contract Code Size Limit](https://eips.ethereum.org/EIPS/eip-170)
- [EIP-2098: Compact Signature Representation](https://eips.ethereum.org/EIPS/eip-2098)
- [EIP-3529: Reduction in Refunds](https://eips.ethereum.org/EIPS/eip-3529)
- [EIP-7623: Increase Calldata Cost](https://eips.ethereum.org/EIPS/eip-7623)
- [ERC-4337: Account Abstraction Using an Alternative Mempool](https://eips.ethereum.org/EIPS/eip-4337)

## Appendix C. Result Reproducibility Notes

The temporary harnesses and raw call traces used for the original Section 5 ledger were removed after measurement, and no protocol files were changed. Those missing original artifacts mean the complete historical table and component call tree cannot be reproduced from Git history alone. The historical rows therefore remain a provisional, commit-pinned ledger rather than a CI-generated golden file.

A follow-up opt-in benchmark now exists at [`tests/gas_profiling/test_transfer_gas_profile.py`](../../tests/gas_profiling/test_transfer_gas_profile.py). It enforces transaction-boundary storage/access normalization, reports gross gas, raw and applied refunds, intrinsic gas, and receipt-like gas, and regression-gates its principal baselines. It independently reproduces the core historical subset below; exact reproduction does not imply that every emitted row is also a regression gate.

```bash
GAS_PROFILE=1 python3 -m pytest tests/gas_profiling/test_transfer_gas_profile.py -q -s
```

Run it as a standalone pytest process because its transaction-boundary mechanism intentionally mutates the shared Titanoboa session journal. The harness is forward-only and scenario-order-dependent; native, cheque, batch, cross-block, and Billing follow-ups therefore run on evolved state unless their labels say otherwise.

The current receipt-like regression baselines use a ±2.5% tolerance and cover owner-whitelist first/same-block/cross-block, matching-primary owner-payee same-block/cross-block repeats, generic-agent same-block/cross-block repeats, historical owner pay-existing-cheque, mature owner cheque create/pay, mature atomic cheque, mature two-transfer batch, and no-primary Billing first/repeat. Other rows can reproduce exactly while remaining emitted observations rather than regression gates; cheque refund assertions are exact, while batch saving also uses the ±2.5% tolerance.

- Owner whitelist first and same-block repeat exactly, plus the historical full-balance gross execution.
- Owner matching-primary-asset payee first and same-block repeat exactly.
- Generic-agent whitelist first and same-block repeat exactly.
- Tracked-native transfers to empty and existing recipients exactly.
- Owner cheque creation and owner payment of an existing cheque exactly; the payment is gated, while historical first-use creation is emitted but not gated.

It also adds separately labeled follow-up variants:

- Owner no-primary-asset payee first/same-block repeat.
- Cross-block owner-whitelist, primary-payee, and generic-agent repeats on an evolved wallet.
- Untracked native on a wallet whose other asset/points state is already initialized.
- Billing no-primary-asset payee pulls.
- Sequential same-block evolved-wallet mature cheque-accounting-class owner two-step and agent atomic routes; these are not cloned-state comparators.
- A sequential mature-manager-state two-transfer agent batch; recipient storage classes match, but exact state is not cloned.

The maintained benchmark does **not** yet reproduce or regression-gate every historical row. Remaining gaps include:

- Signed-agent transfers and generic-agent-to-payee transfers.
- Billing matching-primary-asset payee and Billing cheque-pull routes.
- Agent payment of an existing cheque.
- The first-use atomic-cheque state reported in Section 5.
- The historical first-manager-use batch state and historical direct-manager first/repeat rows.
- The fully fresh untracked-native state and historical full-balance `24,000`-refund state.
- Permanent exclusive component/call-tree capture.
- Cloned-state same-block/cross-block A/B measurements, deeper multi-block points accrual, and period-rollover coverage.
- Base-fork validation with production token/oracle implementations and production UserOperation telemetry.

Newer variant/state results supplement rather than overwrite the figures pinned to commit `cce97d56b62f58c0c3eeddb8fc551d1723bf0fc3`. Any future benchmark update should preserve raw output, name the exact storage/caller/block state, and record divergences instead of silently replacing historical numbers.
